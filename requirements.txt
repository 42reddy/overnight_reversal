# Kotak Neo Trade API — trading (orders, cancels, positions). PyPI package
# name is "kotakneoapi" (the current officially-maintained SDK, published
# from github.com/Kotak-Neo/kotak-neo-python — the old
# Kotak-Neo/Kotak-neo-api-v2 git repo this used to be pinned to is legacy
# and no longer updated); it still exposes the "neo_api_client" import path.
kotakneoapi>=3.0.1
upstox-python-sdk>=2.0.0
pyotp>=2.9.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.8.0
pytz>=2024.1
requests>=2.31.0
python-dotenv>=1.0.0
streamlit>=1.36.0
plotly>=5.20.0
